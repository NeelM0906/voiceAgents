app = "voice-agents-worker"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

[env]
  NODE_ENV = "production"

[processes]
  app = "node dist/agent.js start"

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 1024
